# OrcaSync configuration

# Comma-separated list of subfolders to watch inside ORCA_BASE_PATH
# Default: filament,machine,process
# ORCA_WATCH_FOLDERS=filament,machine,process

# Base path to OrcaSlicer profile directory
# If unset, a cross-platform default is used:
# - Windows: %USERPROFILE%\AppData\Roaming\OrcaSlicer\user\default
# - macOS:   ~/Library/Application Support/OrcaSlicer/user/default
# - Linux:   ~/.config/OrcaSlicer/user/default
# ORCA_BASE_PATH=

# Debounce delay for batching events (ms)
# ORCA_DEBOUNCE_DELAY=500

# Max retries for failed sync attempts
# ORCA_MAX_RETRIES=3

# Base delay used for exponential backoff (ms)
# ORCA_RETRY_DELAY=1000

# Logging level for Winston (error,warn,info,verbose,debug,silly)
# ORCA_LOG_LEVEL=info

# S3/MinIO settings
# You can use either S3_* or legacy MINIO_* variables (S3_* take precedence)
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=
# S3_REGION=eu-west-hetzner
# S3_ENDPOINT=minio.hagenfaber.eu
# Legacy fallbacks (optional):
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
# MINIO_BUCKET_NAME=
